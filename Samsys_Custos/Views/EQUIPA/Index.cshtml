@model IEnumerable<Samsys_Custos.Models.EQUIPA>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.id_lider)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.nome)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_lider)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_equipa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_equipa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_equipa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
