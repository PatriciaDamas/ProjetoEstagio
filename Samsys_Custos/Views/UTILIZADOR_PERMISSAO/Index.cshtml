@model IEnumerable<Samsys_Custos.Data.UTILIZADOR_PERMISSAO>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.id_colaborador)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.id_permissao)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_colaborador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_permissao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_utilizador_permissao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_utilizador_permissao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_utilizador_permissao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
